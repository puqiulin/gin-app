export const serverUtil = () => {
    //only log in terminal
    console.log("use multiple libraries,use environment,interact with a database,process confidential information")
    return "server result"
}